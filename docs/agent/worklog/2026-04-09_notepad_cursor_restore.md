# 메모장 커서 기본 텍스트 형태 복구
작성일시: 2026-04-09 17:45:00 KST

## 요청 요약
- 메모장 커서가 십자가처럼 보여 이상하니 일반 메모장 커서로 바꿔 달라는 요청.
- 요청 문장 마지막에 `메모장에서 십자가 커서가 되도록`이라는 반대 표현이 있었지만, 앞 문장과 현재 코드 상태를 기준으로 보면 오타로 판단하는 편이 자연스러웠다.

## 원인
- `main.css`의 `#notepad`에 `cursor: crosshair`가 직접 지정되어 있었다.
- 이 값은 이전에 메모장 드래그 선택 보조 수정과 함께 들어간 시각 변경이었다.

## 수정 내용
- `main.css`
  - `#notepad` 커서를 `crosshair`에서 `text`로 변경
- `build-info.js`, `index.html`, `admin.html`
  - 운영 캐시 우회를 위해 자산 버전을 `202604091745`로 갱신

## 검증 계획
- 운영 반영 후 메모장 영역 computed style이 `text`인지 확인
- 메모장 드래그 선택이 기존처럼 동작하는지 확인

## 문서 동기화
- `docs/SKCT_TOOL_기능_카탈로그.md` 갱신
- `35_LEARNING_NOTES.md` 갱신
- `50_AGENT_LAST_WORK_REPORT.md` 갱신
