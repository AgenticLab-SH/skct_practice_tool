# SKCT Tool 최근 운영 반영 리포트
작성일시: 2026-04-10 21:04:27 +09:00

이 문서는 최근 운영 반영 기준을 빠르게 이어보기 위한 요약 기록입니다.

## 2026-04-11 운영 반영 완료: OMR 하단 버튼 hover 흔들림 안정화
- 공개 배포 브랜치 `public-clean`은 `c8a755b` 기준으로 다시 반영됐습니다.
- 원인은 OMR 하단 버튼 hover에 걸려 있던 `transform: scale(1.02)`이었습니다.
- 버튼 hover는 이제 크기를 바꾸지 않고, 밝기와 그림자만 바꾸도록 정리해 포인터 경계에서 위아래로 흔들리는 체감을 줄였습니다.
- 캐시 잔존을 막기 위해 `build-info.js`, `main.js` fallback, `index.html`, `admin.html`, `study-archive.html`, `extension-info.html`, `staging/site/index.html`의 자산 버전을 `202604112121`, `v2026.04.11.2121`로 올렸습니다.
- GitHub Pages builds API에서 최신 빌드가 `c8a755b` 기준 `built` 상태인 것을 확인했고, 라이브 `build-info.js`도 `v2026.04.11.2121`를 반환했습니다.

## 2026-04-11 운영 반영 완료: 고급 OMR 안내 축소 및 정오표 일괄입력 채점 후 노출
- 공개 배포 브랜치 `public-clean`은 `e70cae1` 기준으로 다시 반영됐습니다.
- OMR 하단 중간 설명 박스는 제거했고, 이제 고급 복기 안내는 `접기` 버튼 오른쪽의 작은 `?` 버튼으로만 엽니다.
- `정오표 일괄입력`은 더 이상 `정답 입력` 상태에 들어가기만 해도 보이지 않고, `채점` 버튼을 실제로 누른 뒤에만 복기 버튼 구역에서 나타나게 바뀌었습니다.
- 캐시 잔존을 막기 위해 `build-info.js`, `main.js` fallback, `index.html`, `admin.html`, `study-archive.html`, `extension-info.html`, `staging/site/index.html`의 자산 버전을 `202604112107`, `v2026.04.11.2107`로 올렸습니다.
- GitHub Pages builds API에서 최신 빌드가 `e70cae1` 기준 `built` 상태인 것을 확인했고, 라이브 `build-info.js`도 `v2026.04.11.2107`를 반환했습니다.
- 라이브 응답 기준으로 `advancedCoachHelpBtn`, `advancedScoringActionsUnlocked`, `bulkCorrectImportBtn.classList.toggle('hidden', !showAdvancedScoringActions)`가 모두 반영된 것을 확인했습니다.

## 2026-04-11 운영 반영 완료: 정오표 일괄입력 클릭 capture 보강
- 공개 배포 브랜치 `public-clean`은 `5b956ba` 기준으로 다시 반영됐습니다.
- 사용자 참고 이미지 기준으로 재검증한 결과, 기능은 살아 있었지만 기존 구현이 문서 버블링 단계 click 위임에만 기대고 있어 특정 팝업/세션에서는 무반응처럼 보일 여지가 있었습니다.
- `bulkCorrectImportBtn`, `bulkCorrectImportParseBtn`, `bulkCorrectImportApplyBtn`는 이제 capture 단계 위임과 버튼 직접 바인딩을 함께 사용하고, 같은 클릭에서 중복 실행은 막도록 보강했습니다.
- 캐시 잔존을 막기 위해 `build-info.js`, `main.js` fallback, `index.html`, `admin.html`, `study-archive.html`, `extension-info.html`, `staging/site/index.html`의 자산 버전을 `202604112045`, `v2026.04.11.2045`로 올렸습니다.
- GitHub Pages builds API에서 최신 빌드가 `5b956ba` 기준 `built` 상태인 것을 확인했고, 라이브 `build-info.js`도 `v2026.04.11.2045`를 반환했습니다.
- 라이브 팝업 유사 뷰포트에서도 `정오표 일괄입력` 클릭 후 모달이 실제로 열리는 것을 확인했습니다.

## 2026-04-11 운영 반영 완료: 정오표 일괄입력 라벨 siteText 동기화
- 공개 배포 브랜치 `public-clean`은 `872ae95` 기준으로 다시 반영됐습니다.
- 원인은 HTML이 아니라 `site-text-config.js` 기본값 `tools.bulkImportButton`이 여전히 `📥 정오표 입력`으로 남아 있던 것이었습니다.
- `site-text-config.js` 기본값과 관련 안내 문구를 `정오표 일괄입력`으로 통일했고, legacy migration도 예전 문구 `📥 정오표 입력`을 새 기본값으로 승격하도록 보강했습니다.
- 캐시 잔존을 막기 위해 `build-info.js`, `main.js` fallback, `index.html`, `admin.html`, `study-archive.html`, `extension-info.html`, `staging/site/index.html`의 자산 버전을 `202604112035`, `v2026.04.11.2035`로 올렸습니다.
- GitHub Pages builds API에서 최신 빌드가 `872ae95` 기준 `built` 상태인 것을 확인했고, 라이브 `build-info.js`도 `v2026.04.11.2035`를 반환했습니다.
- 라이브 메인 HTML과 `site-text-config.js` 응답 모두 `📥 정오표 일괄입력` 기준으로 반영된 것을 확인했습니다.

## 2026-04-11 운영 반영 완료: 고급모드 정오표 일괄입력 클릭 보강
- 공개 배포 브랜치 `public-clean`은 `31a4364` 기준으로 다시 반영됐습니다.
- 고급모드 `정오표 일괄입력` 버튼과 `붙여넣기 분석`, `정답 일괄 반영`은 이제 직접 DOM 바인딩 대신 `id` 기준 위임 클릭으로 연결됩니다.
- 버튼 문구를 `📥 정오표 일괄입력`으로 통일했고 `type="button"`을 명시해 재렌더/캐시 혼선 환경에서도 동작이 끊기지 않게 보강했습니다.
- 캐시 잔존을 막기 위해 `build-info.js`, `main.js` fallback, `index.html`, `admin.html`, `study-archive.html`, `extension-info.html`, `staging/site/index.html`의 자산 버전을 `202604112005`, `v2026.04.11.2005`로 올렸습니다.
- GitHub Pages builds API에서 최신 빌드가 `31a4364` 기준 `built` 상태인 것을 확인했고, 라이브 `build-info.js`도 `v2026.04.11.2005`를 반환했습니다.
- 라이브 메인 HTML은 `main.css/site-text-config.js/main.js?v=202604112005`, 공개 `admin.html`은 차단 페이지 기준 `200 OK`를 확인했습니다.

## 2026-04-10 운영 반영 완료: 메모장 drag native selection 복귀
- 공개 배포 브랜치 `public-clean`은 `415950f` 기준으로 다시 반영됐습니다.
- 메모장 custom drag 보조 로직을 제거하고, 브라우저 기본 textarea selection으로 다시 돌렸습니다.
- 대신 계산기 표면이 drag 직후 포커스를 가져가며 끊겨 보이지 않도록, 메모장 상호작용 직후에만 짧은 suppression을 유지했습니다.
- 캐시 잔존을 막기 위해 `build-info.js`, `main.js` fallback, `index.html`, `admin.html`, `study-archive.html`, `extension-info.html`, `staging/site/index.html`의 자산 버전을 `202604102104`, `v2026.04.10.2104`로 올렸습니다.
- 로컬 검증에서 메모장 drag는 `selectedLength 4536 / scrollTop 1880`, 클릭 회귀는 `selectedLength 0`을 확인했습니다.
- 라이브 `build-info.js`는 `v2026.04.10.2104`, 공개 `admin.html`은 차단 페이지 기준 `200 OK`, 라이브 메모장 drag는 `selectedLength 4894 / scrollTop 1880`, 클릭 회귀는 `selectedLength 0`을 반환했습니다.

## 2026-04-10 운영 반영 완료: TODO1 문구 정리 및 재배포
- 공개 배포 브랜치 `public-clean`은 `b7f8bca` 기준으로 다시 반영됐습니다.
- `index.html`, `main.js`, `site-text-config.js`에 남아 있던 일반/고급 안내 문구와 버튼 라벨 정리를 묶어 다시 배포했습니다.
- 캐시 잔존을 막기 위해 `build-info.js`, `main.js` fallback, `index.html`, `admin.html`, `study-archive.html`, `extension-info.html`, `staging/site/index.html`의 자산 버전을 `202604101645`, `v2026.04.10.1645`로 올렸습니다.
- 로컬 검증에서 메모장 `전체 선택 후 클릭 -> selectedLength 0`, `긴 드래그 -> selectedLength 4309, scrollTop 1880`을 다시 확인했습니다.
- GitHub Pages builds API에서 최신 빌드가 `b7f8bca` 기준 `built` 상태인 것을 확인했고, 라이브 `build-info.js`도 `v2026.04.10.1645`를 반환했습니다.
- 라이브 `admin.html`은 차단 페이지 기준 `200 OK`, 라이브 메모장 클릭 테스트는 `selectedLength 0`을 반환했습니다.

## 2026-04-10 운영 반영 완료: 일반·고급 안내 흐름 통합 정리
- 공개 배포 브랜치 `public-clean`은 `1a039d9` 기준으로 다시 반영됐습니다.
- 일반 도움말, 고급 신청, 고급 활용, 고급 OMR 하단 안내를 `한 줄 흐름 요약 -> 핵심 조작 -> 접힌 세부 설명 -> ? 빠른 도움말` 구조로 다시 맞췄습니다.
- 일반 모드와 고급 모드에서 겹치던 안내 문구는 줄이고, 같은 설명이 여러 모달에 반복되던 부분은 역할별로 나눴습니다.
- 일반 도움말은 예시와 참고 블록을 접힘 구조로 옮기고, 고급 신청과 고급 활용은 각각 `신청/승인/진입`, `들어온 뒤 사용 순서`만 보이게 정리했습니다.
- 고급 OMR 하단은 `고급 복기 / 정답 입력 -> 채점 -> 복기 버튼 / 전체 안내 / ?` 형태의 짧은 레이아웃으로 바뀌었습니다.
- 운영 `siteTextConfig`가 예전 장문을 다시 덮지 않도록 `site-text-config.js` 기본값과 legacy migration도 함께 보강했습니다.
- 라이브 검증에서 `build-info.js`는 `v2026.04.10.1313`, `assetVersion 202604101313`, 메인 HTML은 `main.css/site-text-config.js/main.js?v=202604101313`, 공개 `admin.html`은 차단 페이지 기준 `200 OK`를 확인했습니다.

## 2026-04-10 운영 반영 완료: 메모장 클릭 전체선택 오동작 복구
- 공개 배포 브랜치 `public-clean`은 `92f31d1` 기준으로 다시 반영됐습니다.
- 일반 모드와 고급 모드는 같은 `#notepad` 입력창 로직을 공유하고 있었고, 이번 수정도 공통 경로에 반영했습니다.
- 원인은 메모장 drag auto-scroll 보조가 `pointerdown` 즉시 drag selection 상태를 켜고, native caret 이동과 충돌하던 구조였습니다.
- 메모장 선택 보조는 이제 실제 드래그 이동이 threshold를 넘겼을 때만 켜지고, 단순 클릭은 일반 textarea처럼 caret만 이동하게 정리했습니다.
- pointer 이벤트와 mouse 이벤트를 동시에 중복으로 걸던 구조도 pointer 우선 / mouse fallback 구조로 줄였습니다.
- 로컬 검증에서 `전체 선택 상태에서 클릭 -> selectedLength 0`, `실제 드래그 -> selectedLength 608, scrollTop 378`을 확인했습니다.
- 라이브 검증에서 `build-info.js`는 `v2026.04.10.1124`, `assetVersion 202604101124`, 메모장 클릭 테스트는 `selectedLength 0`을 반환했습니다.

## 2026-04-10 로컬 관리자 실행 개선: Python 런처 기준으로 정리
- 로컬 관리자 진입 기본 명령을 `py scripts/open_local_admin.py`로 바꿨습니다.
- 종료도 `py scripts/stop_local_admin.py`로 맞췄습니다.
- `open_local_admin.cmd`, `stop_local_admin.cmd`는 이제 같은 Python 스크립트를 호출하는 호환용 래퍼입니다.
- VS Code `SKCT: 로컬 관리자 페이지 열기/종료` 작업도 `py` process task로 바꿔 같은 Python 런처를 직접 실행합니다.
- 실제 확인에서 `py scripts/open_local_admin.py --no-browser`가 `http://127.0.0.1:8135/admin.html`을 정상으로 열고, 관리자 URL `200 OK` 응답을 확인했습니다.
- 이 변경은 로컬 운영 편의 개선이며, 공개 GitHub Pages 재배포는 포함하지 않습니다.

## 2026-04-10 운영 반영 완료: 관리자 `config` 저장 권한 복구 및 상태 배너 보강
- 운영 RTDB rules에 `config/.write = auth != null`를 실제 반영해, 로그인된 관리자 기준의 `공지`, `기본 설정`, `후원/운영 문구`, `사이트 텍스트`, `수동 구독 설정` 저장이 다시 정상 동작하도록 복구했습니다.
- 원인은 `config/$key/.write` wildcard만으로는 `notice_sidebar` 같은 명시적 자식 노드 저장이 풀리지 않았던 rules 구조였습니다.
- 로컬 관리자 화면 상단에는 `세션 상태`와 `마지막 저장 결과`를 함께 보여주는 상태 배너를 추가했습니다.
- 공지 저장과 타이머/레이아웃 저장은 실제 로컬 관리자 로그인 상태에서 다시 눌러 성공 토스트와 상단 상태 갱신을 확인했습니다.
- 신청 승인/반려/차단/삭제, 백업 복원, 키 생성, 테스트 기본값 저장/실서버 반영도 같은 상태줄과 실패 토스트를 쓰도록 정리했습니다.
- 관리자 로그인 화면은 `<form>` submit 구조로 바꿔 Enter 제출이 자연스럽게 동작하도록 정리했습니다.
- 이번 반영은 운영 RTDB rules와 로컬 관리자 도구 개선이며, 공개 GitHub Pages `admin.html` 차단 정책은 그대로 유지합니다.

## 2026-04-10 운영 반영 완료: 고급 버튼 복구와 로컬 관리자 실행 오류 수정
- 공개 배포 브랜치 `public-clean`은 `488e025` 기준으로 다시 반영됐습니다.
- 고급 모드 우측 상단 말풍선 버튼은 더 이상 placeholder alert를 띄우지 않고, `고급 활용` 모달을 바로 여는 빠른 진입 버튼으로 바뀌었습니다.
- `더보기 > 자료 보관함`은 새 탭으로만 열리고, 현재 연습 페이지가 같이 이동하던 이중 이동 문제를 제거했습니다.
- 고급 라이선스 활성 직후 `다음 / 과↺ / 전↺` 타이머 버튼 제목과 disabled 상태가 바로 다시 계산되도록 맞췄습니다.
- VS Code 로컬 관리자 작업은 `cmd.exe /c` 기반 process task로 바꿔 `-Command` 오류 없이 바로 실행되게 정리했습니다.
- 라이브 확인에서 `build-info.js`는 `v2026.04.10.0932`, `assetVersion 202604100932`, 메인 URL `200 OK`, 공개 `admin.html` 차단 페이지 `200 OK`를 확인했습니다.

## 2026-04-10 운영 반영 완료: 버튼 내부 장문 설명 축소 및 `?` 빠른 도움말 정리
- 공개 배포 브랜치 `public-clean`은 `33f85d5` 기준으로 다시 반영됐습니다.
- 고급 OMR의 `고급 복기 순서` 인라인 설명은 기본 화면에서 제거하고, `?` 버튼을 누르면 빠른 도움말로 보이게 바꿨습니다.
- 설정, 더보기, 활성 세션, 고급 신청, 고급 활용, 과목별 상세 통계, 정오표 일괄입력은 헤더 `?` 버튼으로 핵심 설명을 다시 볼 수 있게 맞췄습니다.
- 기본 화면에는 조작에 필요한 짧은 문구만 남기고, 중복되던 소제목과 장문 설명은 줄였습니다.
- 고급 신청 도움말과 고급 활용 도움말은 제목이 두 번 보이지 않도록 요약 문구를 다시 정리했습니다.
- 로컬 검증에서는 `?` 도움말이 모두 열리고, 기본 팝업 폭 강제 상태에서도 고급 OMR 입력칸과 하단 버튼 줄이 유지되는 것을 확인했습니다.
- 라이브 확인에서는 `build-info.js`가 `v2026.04.10.0219`, 메인 HTML이 `main.css/main.js/site-text-config.js?v=202604100219`, `admin.html`이 공개 차단 페이지를 반환하는 것을 확인했습니다.

## 2026-04-10 운영 반영 완료: 기본 팝업 폭 보정 및 일반/고급 안내 흐름 재정리
- 공개 배포 브랜치 `public-clean`은 `f25bd51` 기준으로 다시 반영됐습니다.
- 팝업 창은 너무 좁게 열리지 않도록 최소 폭 보정을 넣었고, 실제 팝업 확인에서 안쪽 폭 `520px`, 기본 OMR 폭 `161px` 기준으로 열리는 것을 확인했습니다.
- 팝업 기준 폭에서도 OMR 입력칸과 하단 버튼이 같이 보이도록 OMR 최소 폭과 팝업 전용 버튼/패널 밀도를 함께 조정했습니다.
- `가이드`는 일반 모드 흐름만 설명하고, `고급 안내`는 신청/승인/진입만, `고급 활용`은 이미 들어온 뒤 버튼 사용 순서만 설명하도록 역할을 다시 나눴습니다.
- `고급 안내` 모달은 좁은 폭에서 2열 폼과 카드가 남지 않도록 1열 반응형 구조로 바꿨고, `고급 활용` 모달도 같은 기준으로 정리했습니다.
- 운영 `siteTextConfig`의 예전 안내 문구가 다시 덮지 않도록 `site-text-config.js` 기본값과 legacy migration 기준을 함께 보강했습니다.
- 라이브 확인에서 `build-info.js`는 `v2026.04.10.0149`, 메인 화면 `고급 안내` 모달은 짧아진 진입 문구와 새 신청 흐름을 반환했습니다.

## 2026-04-10 운영 반영 완료: 고급 팝업 OMR 정상화 및 고급 안내 도식화
- 공개 배포 브랜치 `public-clean`은 `98f392a` 기준으로 다시 반영됐습니다.
- 고급 팝업 OMR에서 긴 중간 안내가 하단 버튼을 밀어내던 문제를 줄이기 위해, `고급 복기 순서` 패널을 3단계 압축형으로 바꿨습니다.
- 팝업 모드에서는 긴 설명 문단을 숨기고 `정답 입력 -> 채점 확인 -> 복기 버튼`만 남겨, OMR 입력칸과 하단 고급 버튼이 함께 보이도록 맞췄습니다.
- `고급 기능 빠른 안내` 모달은 텍스트가 깨지던 이미지 2장을 제거하고, `상단 상태 바`와 `OMR 아래 복기 구역`을 보여주는 도식 카드로 다시 구성했습니다.
- 운영 `siteTextConfig`의 예전 고급 안내 문구가 새 레이아웃을 부분적으로 덮지 않도록, `site-text-config.js` 기본값과 legacy migration 기준도 함께 보강했습니다.
- 공개 배포에는 `advanced-tools.html`을 다시 포함해 고급 팝업 인증 흐름을 복구했고, `admin.html`은 계속 차단 안내 페이지로 유지했습니다.
- 라이브 확인에서 `advanced-tools.html`은 `200 OK`, `admin.html`은 차단 페이지 기준 `200 OK`, `build-info.js`는 `v2026.04.10.1045`를 반환했습니다.

## 2026-04-10 운영 반영 완료: public-clean 공개 배포 전환 및 관리자 공개 경로 차단
- GitHub Pages 게시 원본을 `main`에서 `public-clean / (root)`로 전환했습니다.
- 공개 배포 브랜치 `public-clean`은 `0321e08` 기준으로 반영됐습니다.
- 운영 공개 URL은 `https://agenticlab-sh.github.io/skct_tool/` 그대로 유지됩니다.
- 배포 직후 라이브 `admin.html`이 예상과 다르게 실제 관리자 화면을 응답하는 상황을 확인했습니다.
- 이를 바로 수정해, 공개 배포물에는 실제 관리자 화면 대신 차단 안내용 `admin.html`을 명시적으로 포함하도록 바꿨습니다.
- 현재 공개 배포에서 `admin.html`은 더 이상 관리자 대시보드가 아니라 일반 차단 페이지여야 합니다.
- 이번 반영은 정적 공개 경계 정리이며, Firebase Functions 배포와 `secureApiBaseUrl` 저장, 민감 RTDB rules 최종 잠금은 아직 남아 있습니다.

## 2026-04-09 운영 반영 완료: 그림판 커서를 원 없는 얇은 십자로 단순화
- 그림판은 `cursor: none` 기반 커스텀 커서를 유지하되, 가운데 원형 ring 프리뷰는 제거했습니다.
- 십자 선 두께를 1px 수준으로 줄여 일반 얇은 조준점처럼 보이도록 조정했습니다.
- 운영 자산 버전은 `main.css?v=202604091948`, `build-info.js?v=202604091948` 기준으로 갱신했습니다.

## 2026-04-09 운영 반영 완료: 메모장 커서 기본 텍스트 형태 복구
- 메모장 `textarea`에 들어갔던 `crosshair` 커서를 일반 텍스트 입력 커서로 되돌렸습니다.
- 드래그 선택 보조 로직은 유지하되, 커서 모양만 역할에 맞게 분리했습니다.
- 그림판 쪽 `cursor: none` 기반 커스텀 커서 체계는 그대로 유지됩니다.
- 운영 자산 버전은 `main.css?v=202604091745`, `build-info.js?v=202604091745` 기준으로 갱신했습니다.

## 2026-04-09 운영 반영 완료: 관리자 로그인 상태와 실제 쓰기 권한 간극 축소
- 문제 추적 결과, `2026-04-08`의 Firebase Auth 전환 뒤 관리자 화면은 `onAuthStateChanged`의 사용자 객체만 보고 대시보드에 진입하고 있었습니다.
- 하지만 실제 RTDB 쓰기 전에는 토큰 유효성을 다시 확인하지 않아, 화면상 로그인 상태와 실제 `config` 쓰기 가능 상태가 어긋날 수 있었습니다.
- 이를 줄이기 위해 관리자 페이지에 아래 공통 보강을 넣었습니다.
- 대시보드 진입 전 `user.getIdToken()` 확인
- 모든 관리자 쓰기 작업 전 토큰 확인
- `Permission denied` 발생 시 강제 토큰 갱신 후 1회 자동 재시도
- 이 보강은 공지 저장뿐 아니라 기본값 저장, 후원 저장, 게시글/댓글 관리, 신청 승인/반려/삭제, 백업 복원까지 같은 경로로 적용됩니다.

## 2026-04-09 운영 반영 완료: 관리자 저장 실패 원인 노출 보강
- 운영 DB `config/notice_sidebar`에는 새 문구가 들어가지 않았고, 실제 원인은 공개 화면 반영이 아니라 관리자 저장 실패 쪽으로 확인했습니다.
- RTDB `config` 쓰기 규칙은 `auth != null`이라 로그인 세션이 끊기면 `Permission denied`가 납니다.
- 기존 관리자 페이지는 `공지 저장`, `기본값 저장`, `후원 저장` 같은 개별 저장 버튼에서 이 실패를 충분히 알리지 못했습니다.
- 그래서 저장 버튼 공통 래퍼를 추가해, 권한 실패면 `새로고침 후 다시 로그인`, 네트워크 실패면 `연결 확인 후 재시도` 메시지가 토스트로 바로 보이게 했습니다.
- 이 변경은 `admin.html`에 반영했고, 사용자는 관리자 페이지를 새로고침한 뒤 다시 저장하면 실패 이유를 바로 확인할 수 있습니다.

## 2026-04-09 운영 반영 완료: 공지 저장 즉시 반영 복구
- 관리자 페이지 `공지 관리`에서 `사이드바 공지`를 저장하면, 이미 열려 있던 메인 페이지도 `config/notice_sidebar`, `config/notice_help`, `config/notice`를 실시간 구독해 즉시 다시 렌더링되도록 수정했습니다.
- 공개 설정 초기 로딩은 유지하되, 공지 관련 키 하나가 읽기 실패해도 다른 공개 설정까지 같이 멈추지 않게 키별 오류를 분리했습니다.
- Help 모달 상단 공지는 `show=false` 저장 시 예전 내용이 남지 않도록 `devNotice`를 비우는 보정도 함께 넣었습니다.
- 운영 자산 버전은 `build-info.js?v=202604091640`, `main.js?v=202604091640` 기준으로 갱신했습니다.
- 운영 기능 반영 커밋은 `e748b82`입니다.
- 라이브 검증에서 `build-info.js` 버전 `v2026.04.09.1640`, HTML의 `main.js?v=202604091640`, `subscribeLiveNoticeConfig` 포함을 확인했습니다.

## 2026-04-09 운영 반영 완료: 고급 모드 인라인 상태 패널 제거
- 고급 모드 상단에 항상 보이던 `고급 모드 상태` 패널을 제거했습니다.
- 좌측 고급 버튼은 유지해, 고급 기능은 버튼을 눌러 여는 흐름만 남겼습니다.
- 이 변경으로 계산기와 도구 영역 높이를 다시 확보했습니다.
- 운영 자산 버전은 `main.css?v=202604091519`, `main.js?v=202604091519`, `build-info.js?v=202604091519` 기준으로 갱신했습니다.
- 운영 반영 커밋은 `5cbd19e`, GitHub Pages run은 `24175759917`입니다.
- 라이브 검증에서 상태 바 DOM 제거, 좌측 고급 버튼 유지, 계산기 높이 `352px`를 확인했습니다.

## 2026-04-09 운영 반영 완료: 사이드바 공지사항 버튼 + 관리자 공지 분리
- 좌측 사이드바에 빨간 느낌표 `공지` 버튼을 추가했습니다.
- 버튼은 Help 공지와 분리된 `config/notice_sidebar`를 읽고, 별도 공지 모달을 엽니다.
- 관리자 페이지 `공지 관리`에는 `사이드바 공지` 대상을 추가했습니다.
- 버튼 라벨과 공지 모달 제목은 `siteTextConfig`에 새 키를 추가해 관리자 문구 편집기에서도 수정할 수 있게 맞췄습니다.
- 운영 공지 기본 문구는 메모장 개선 안내로 반영했습니다.
- 운영 반영 전 백업은 `_backup/20260409_145900_sidebar_notice_deploy/`에 남겼습니다.
- 첫 라이브 확인에서 `config/notice_sidebar` 읽기 규칙 누락으로 본문이 비는 문제를 확인했고, `database.rules.json`과 RTDB rules를 바로 수정해 정상화했습니다.

## 2026-04-09 운영 반영 완료: 메모장 드래그 끊김 완화 + 십자 커서 + 캐시 버전 갱신
- 메모장 `textarea` 드래그 선택이 하단 경계에서 막힌 것처럼 느껴지던 문제를 줄이기 위해, `requestAnimationFrame` 기반 auto-scroll 보조를 추가했습니다.
- `mouse`와 `pointer` 이벤트를 capture 단계에서 함께 추적해 브라우저별 selection 이벤트 차이를 줄였습니다.
- auto-scroll은 내려가는데 선택 끝점이 멈추는 엔진을 위해, 다음 줄 경계까지 보조 확장하는 fallback을 넣었습니다.
- 메모장 커서는 `crosshair`로 통일해 작은 검은 십자가 느낌으로 보이게 맞췄습니다.
- 드래그 직후 계산기 포커스가 끼어들어 끊김처럼 느껴지지 않도록 짧은 suppression도 추가했습니다.
- 운영 자산 버전은 `main.css?v=202604091417`, `build-info.js?v=202604091417`, `main.js?v=202604091417`, `site-text-config.js?v=202604091417` 기준으로 갱신했습니다.
- 운영 반영 커밋은 `262f73e`, GitHub Pages run은 `24173838768`입니다.
- 라이브 페이지 검증에서 메모장 커서 `crosshair`, drag selection `scrollTop 2946`, `endLine 177`까지 확인했습니다.

## 2026-04-09 운영 반영 완료: 고급 모드 상태 바 + OMR 빠른 설명 + 시각 가이드 이미지
- 고급 모드 상단에 `고급 모드 상태 바`를 추가했습니다.
- 상태 바에는 `활성 여부`, `로그인 ID/이메일`, `만료 시각`, `자료 보관함 가능 여부`, `우측 실제환경 여백 복원 여부`가 바로 보입니다.
- OMR 하단에는 `고급 버튼 빠른 설명` 패널을 추가해, 정답 입력 -> 채점/통계 -> 반복 연습 순서를 초보자 기준으로 읽기 쉽게 정리했습니다.
- 도움말 모달에는 `고급 모드 활용 가이드` 섹션을 추가했고, 강조 박스와 잘라낸 화면을 적용한 이미지 2장을 함께 넣었습니다.
- 사용자 화면과 관리자 화면의 버전 표시는 `build-info.js` 공용 정보로 통일했습니다.
- 운영 Firebase `config/siteTextConfig`도 새 코드 기본값으로 다시 동기화해, 새 고급 안내/상태 바 문구가 운영값에 가려지지 않게 맞췄습니다.
- 운영 정적 자산 버전은 `main.css?v=202604091119`, `build-info.js?v=202604091119`, `main.js?v=202604091119`, `study-archive.js?v=202604091119` 기준으로 반영됐습니다.

## 2026-04-09 운영 반영 완료: 자료 보관함 고급 모드 전용 게이트 + 운영 문구 동기화
- 자료 보관함은 이제 일반 모드 `더보기`에서 숨기고, 고급 모드에서만 노출합니다.
- `study-archive.html` 직접 진입도 막아, 브라우저에 유효한 고급 라이선스가 없으면 접근 차단 화면만 보이도록 바꿨습니다.
- 고급 라이선스가 있더라도 자료보관함 안에서는 다시 로그인한 계정만 자기 자료를 읽고 수정할 수 있습니다.
- 고급 안내에는 고급 모드 전용 기능 목록과 자료 보관함 접근 기준을 체계적으로 추가했습니다.
- 관련 문구는 `site-text-config.js` 기본값과 관리자 문구 편집 대상에 함께 반영했고, 운영 Firebase `config/siteTextConfig`도 새 기준값으로 동기화했습니다.
- 운영 GitHub Pages는 `main.js?v=202604090955`, `site-text-config.js?v=202604090955` 기준으로 빌드 완료했습니다.

## 2026-04-09 운영 반영: TODO 전체 정리 + 로그인 ID/이메일 정리 + REQ 프리필 제거
- `docs/TODO` 전체 일괄 처리분과 고급 로그인 흐름 정리분을 함께 운영 반영 대상으로 묶었습니다.
- 메인 화면 단순화, `더보기` 유틸리티 모달, 자료 보관함 페이지, 관리자 문구 편집기 확장, 그림판 커서 가시성 개선이 포함됩니다.
- 고급 로그인은 이제 `신청 이메일 또는 로그인 ID + 비밀번호`만 사용하고, 신청 조회는 `신청 이메일 + 조회 비밀번호`로만 받습니다.
- 운영 사용자가 본 `REQ-MNQ8YWJO-I37J` 자동채움 문제는 예전 `localStorage` 레거시값 fallback 때문이었고, 이번에 이메일이 아닌 최근 신청값은 자동으로 삭제되도록 정리했습니다.
- 운영 HTML에서는 `site-text-config.js`, `main.js` 버전을 올려 새 JS와 새 문구 파일을 강제로 다시 받게 했습니다.
- 이전에 로컬에만 남아 있던 `database.rules.json` 변경분도 함께 운영 배포 대상으로 포함했습니다.

## 2026-04-09 운영 반영 대기: TODO 전체 일괄 처리
- 이번 라운드에서는 운영 서버를 건드리지 않고 로컬 코드와 문서 기준으로 TODO 전체를 일괄 처리했습니다.
- 작업 시작 전 안전 백업을 먼저 만들었습니다.
- 작업 브랜치: `work/todo-sweep-20260409_0215`
- 백업 커밋: `4b40945`
- 백업 태그: `backup-20260409-0215-todo-sweep-start`
- 파일 백업: `_backup/20260409_0215_todo_sweep_start/`
- 메인 화면은 핵심 진입만 남긴 사이드바와 `더보기` 유틸리티 모달 구조로 단순화했습니다.
- 일반 안내와 고급 기능 모달 문구는 사용자 행동 중심으로 다시 정리했습니다.
- 관리자 문구 편집기에는 scope 필터와 선택 힌트를 추가하고, 레거시 운영 기본값 자동 보정 로직을 넣었습니다.
- 그림판에는 커스텀 커서 오버레이를 넣어 밝은 배경에서도 가시성이 유지되게 했습니다.
- `study-archive.html` 전용 개인 학습자료 보관함과 사용자별 RTDB rules 경계를 추가했습니다.
- 세션성 민감값은 `sessionStorage`로 옮겨 브라우저에 오래 남지 않게 줄였습니다.
- 아직 남은 것은 사용자 승인 후 배포, RTDB rules 실제 반영, 운영 Auth와 보관함 실계정 검증입니다.

## 2026-04-09 운영 반영 예정: 신청번호 사용자 비노출 및 이메일 조회 전환
- 이용권 신청 조회는 이제 사용자 기준으로 `이메일 + 조회 비밀번호` 중심 흐름으로 정리했습니다.
- 신청번호는 Firebase 내부 식별과 관리자 작업용으로만 남기고, 사용자 신청/조회 결과/고급 모드 안내에서는 숨기도록 코드를 조정했습니다.
- 새 신청 저장 시에는 `subscriptionRequests/<requestId>` 외에 `subscriptionRequestLookup/<sha256(email::password)>`도 같이 저장합니다.
- 이 lookup 저장이 실패하면 신청 전체를 되돌리고 실패 처리하므로, 성공한 신청은 항상 이메일 기준 조회가 가능해야 합니다.
- 관리자 페이지에서 신청 삭제 시 lookup 인덱스도 같이 제거되도록 맞췄습니다.
- 원격 `config/siteTextConfig`는 코드 기본값으로 다시 동기화해, 예전 `신청번호` 안내 문구가 운영 화면에 남지 않도록 정리했습니다.

## 2026-04-09 운영 반영 예정: 실시간 접속자 정리 버튼 의미 수정 및 고급 구독 저장 권한 복구
- 관리자 페이지의 `실시간 접속자 초기화`는 실제 활성 세션까지 없애는 기능처럼 보였지만, 운영상 필요한 건 stale/legacy 세션 정리였다.
- 버튼을 `유령 세션 정리`로 바꾸고, 최근 150초 안 하트비트를 보낸 세션은 유지한 채 오래된 값만 삭제하도록 수정했다.
- 운영 `active_visitors`는 정리 후 stale/legacy 0건 상태를 확인했다.
- 고급 구독 저장 시 `Permission denied`가 나던 원인은 `advancedAccountLicenses` 루트 쓰기 권한이 rules에 없었기 때문이다.
- `advancedAccountLicenses/.write = auth != null` 규칙을 추가해, 기존 사용자 만료일 수정과 새 수동 구독 저장이 다시 가능하도록 복구했다.

## 2026-04-09 운영 반영 예정: 운영 DB 정리 및 신청 카드 개별 삭제 강화
- 사용자 승인에 따라 운영 DB에서 아래 항목을 정리했습니다.
  - `config/advancedFeatureConfig`의 임시 수동 계정 `esun`, `sh`
  - 오래된 `pending` 신청 `REQ-MNPSNTEB-GVLO`
  - 중복 반려 이력 7건
- 정리 전 백업은 `_backup/20260409_002351_live_cleanup_approved/` 아래에 남겼습니다.
- 정리 후 운영 기준은 아래처럼 단순화됐습니다.
  - 고급 구독 내역: `ods1106`, `mingyu7275`
  - 수동 구독 신청: 위 두 건의 `fulfilled` 요청만 유지
- 관리자 페이지의 신청 목록은 이제 복호화 전 상태여도 카드마다 `삭제` 버튼이 바로 보입니다.

## 2026-04-09 운영 반영 완료: 수동 고급 계정의 실제 로그인 경로 복구
- 문제의 핵심은 `고급 구독 내역` 저장과 공개 페이지 로그인 경로가 서로 달랐다는 점입니다.
- 관리자 페이지는 `config/advancedFeatureConfig`에 계정을 저장했지만, 공개 페이지는 그 경로를 더 이상 로그인에 쓰지 않아 `esun` 같은 수동 계정이 즉시 로그인되지 않았습니다.
- 이를 해결하기 위해 `advancedAccountLicenses/<loginId>` 공개 로그인 레코드를 새로 도입했습니다.
  - 관리자 저장 시 서명된 라이선스 번들을 만들고
  - 그 번들을 계정 비밀번호로 다시 암호화해 저장하며
  - 공개 페이지는 이 레코드를 읽어 비밀번호로 복호화한 뒤 공개키로 서명을 검증합니다.
- 이 구조 덕분에 비밀번호 해시를 공개하지 않으면서도 수동 추가 ID/비밀번호 로그인이 다시 동작합니다.
- 세션용 평문 보조값 `_plainPasswordLocal`은 관리자 화면 메모리에만 남기고, Firebase 저장본에서는 제거하도록 정리했습니다.
- 운영 RTDB `advancedAccountLicenses`에는 신청 원본에서 비밀번호를 복구할 수 있는 `ods1106`, `mingyu7275` 레코드를 먼저 재생성했습니다.
- `esun`, `sh`는 해시만 남아 있어 평문 비밀번호를 역산할 수 없으므로, 운영자가 비밀번호를 한 번 다시 입력해 저장해야 실제 로그인도 살아납니다.

## 2026-04-08 운영 반영 완료: 관리자 페이지 탭 재구성 및 키 안내 정리
- 관리자 페이지를 긴 단일 스크롤 대신 탭 구조로 재구성했습니다.
  - `🔐 구독`
  - `⚙️ 기본 설정`
  - `📝 문구/공지`
  - `💬 커뮤니티`
  - `🧭 운영 개요`
- 빠른 이동 버튼은 이제 대상 카드가 있는 탭을 먼저 연 뒤 해당 위치로 이동합니다.
- `신청 복호화 키`와 `라이선스 서명 키`의 역할을 실제 운영 흐름 기준으로 다시 설명하고, 표준 저장 경로를 관리자 화면에 직접 표시했습니다.
- 운영 HTTPS 관리자 페이지에서는 로컬 키 브리지를 자동 시도하지 않도록 바꿨습니다. 기본 흐름은 `키 폴더 연결`이며, 로컬 브리지는 개발용 예비 도구로만 남겼습니다.
- `고급 구독 내역`의 비밀번호 `확인` 버튼은 관리자 암호문이 없는 계정이라도, 메모 안에 연결된 신청번호가 있고 신청 복호화 키가 준비되어 있으면 수동 신청 원본에서 현재 비밀번호를 다시 보여줄 수 있습니다.

## 2026-04-08 운영 반영 완료: 수동 구독 중복 방지 및 승인 동기화
- 수동 구독 신청 폼에 제출 중 잠금과 최근 동일 신청 차단을 넣어, 같은 브라우저에서 짧은 시간 안에 같은 신청이 여러 번 들어가는 문제를 줄였습니다.
- 관리자 승인 버튼은 이제 라이선스만 발급하지 않고 `config/advancedFeatureConfig`의 `고급 구독 내역`까지 같이 갱신합니다.
- 승인 카드의 기본 만료일 계산 기준을 `현재 시각`이 아니라 `신청자가 고른 시작일 + 이용권 일수`로 보정했습니다.
- 운영 RTDB의 실제 중복 신청 2묶음을 정리했습니다.
  - `ods1945@naver.com` 최신 신청 `REQ-MNPYGTBA-B5Y4` 승인, 이전 5건 반려
  - `mingyu7275@naver.com` 최신 신청 `REQ-MNQ2I11P-IIXB` 승인, 이전 2건 반려
- 운영 RTDB `config/advancedFeatureConfig`에는 `ods1106`, `mingyu7275` 두 계정이 실제 저장됐고, 두 승인 신청 모두 사용자 비밀번호 경로 복호화 및 라이선스 서명 검증을 통과했습니다.
- 자세한 데이터 처리 기록은 `docs/agent/worklog/2026-04-08_manual_subscription_duplicate_audit_and_approval.md`를 참고합니다.

## 2026-04-08 운영 반영 완료: 보안 강화 및 인증 구조 정리
- 관리자 로그인은 공개 RTDB 해시 비교 대신 Firebase Auth 기준으로 정리했습니다.
- 고급 기능 해금은 로컬 저장소 플래그가 아니라 서명된 라이선스 번들 검증 구조로 바꿨습니다.
- 수동 신청 승인 흐름은 고급 계정을 DB에 직접 추가하는 대신, 신청별 라이선스를 발급해 사용자 브라우저에서 검증하는 방식으로 바뀌었습니다.
- 운영/스테이징 페이지의 공지, 후원 문구, 링크 렌더링 경로에 sanitize 를 적용해 저장형 XSS 재현 경로를 막았습니다.
- 공개 페이지는 더 이상 Firebase `config` 루트를 통째로 읽지 않고, 공개가 필요한 키만 개별 조회합니다.
- `database.rules.json` 기준 RTDB rules 배포까지 완료했습니다.
- 운영 RTDB `config/manualSubscriptionConfig`에는 라이선스 검증 공개키를 추가했고, `config/siteTextConfig/advancedGuide`, `config/siteTextConfig/messages`도 최신 흐름 기준으로 동기화했습니다.
- 운영 RTDB의 레거시 민감 경로 `config/adminHash`, `config/advancedFeatureConfig`는 실제 DB에서도 제거했습니다.

## 확인 메모
- GitHub Pages와 Firebase RTDB rules 반영까지 모두 끝났습니다.
- 공개 검증 결과 `config.json`, `config/adminHash.json`, `config/advancedFeatureConfig.json`은 비인증 `401`, `config/appName.json`은 공개 `200`입니다.
- 자세한 수정/검증 기록은 `docs/agent/worklog/2026-04-08_security_hardening_and_public_config_lockdown.md`를 우선 참고합니다.
